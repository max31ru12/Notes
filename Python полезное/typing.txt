------------------------------------------------------------------------------------------------

import enum

class Colors(enum.Enum): - Enum используется для перечислений (используется для констант)
    red = 1
    green = 2
    blue = 3
------------------------------------------------------------------------------------------------

from typing import Annotated

intpk = Annotated[int, mapped_column(primary_key=True)]
        # int - тип переменной
        # второй агрумент - это метаинформация о том, что это например mapped_column(primary_key=True)