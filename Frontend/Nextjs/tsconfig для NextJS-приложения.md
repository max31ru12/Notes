

## Готовый файл

```json
{
  "compilerOptions": {
    /* Базовые цели и окружение */
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    "noEmit": true,

    /* React / JSX и Next.js */
    "jsx": "preserve",

    /* Импорты и модули */
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": true,
    "skipLibCheck": true,

    /* Строгий режим и проверки */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,

    /* Доп. флаги из Vite-конфигов */
    "useDefineForClassFields": true,
    "erasableSyntaxOnly": true
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": [
    "node_modules"
  ]
}

```


- `"target": "ES2020"`  
    До какого стандарта JS компилить TypeScript.  
    В Vite-конфиге у тебя было `ES2020` (app) и `ES2022` (node).  
    В Next-клиентском коде достаточно `ES2020`. Серверный код под Next тоже живёт в современном окружении, но чаще те используют один общий target.
    
- `"lib": ["ES2020", "DOM", "DOM.Iterable"]`  
    Набор доступных глобальных типов (Promise, DOM API, и т.д.).
    
    - `ES2020` — стандартные объекты и методы JS.
        
    - `DOM`, `DOM.Iterable` — типы для браузерного DOM и итерируемых коллекций.  
        Это взято из твоего первого tsconfig (app), во втором (node) был только `ES2023`, но для Next важно наличие DOM-типов, т.к. большая часть кода — клиентская.
        
- `"module": "ESNext"`  
    Какой модульный формат использовать на уровне TypeScript (ESM).  
    Совпадает с обоими твоими tsconfig (и app, и node). Next/Turbopack/webpack потом уже делают своё.
    
- `"moduleResolution": "bundler"`  
    Новый режим резолвинга модулей, оптимизированный под сборщики (Next/Turbopack/Vite и т.п.), позволяет:
    
    - лучше работать с ESM/импортами,
        
    - аккуратнее обрабатывать package.json `exports`/`imports`.  
        В обоих твоих конфигурациях это уже стоит — оставляем.
        
- `"moduleDetection": "force"`  
    Говорит TypeScript: _рассматривай каждый файл как модуль, если там есть import/export_.  
    Это ты явно включал в обоих конфигax — вполне ок и для Next.
    
- `"noEmit": true`  
    TypeScript не должен генерировать `.js`-файлы — всю эмиссию делает Next (через Turbopack/Webpack).  
    У тебя это уже стояло в обоих tsconfig, для Next это тоже стандартный подход.
    

---

### ⚛️ React / JSX и интеграция с Next.js

- `"jsx": "preserve"`  
    В твоём Vite-конфиге было `"react-jsx"`, т.к. Vite сам использует новый JSX runtime.  
    Для Next нужна именно `"preserve"`:
    
    - TS не трогает JSX,
        
    - Next сам трансформирует JSX (серверные компоненты, client components, `use client`/`use server` и т.д.).
        

---

### 📦 Импорты, модули, TS-расширения

- `"verbatimModuleSyntax": true`  
    Требует, чтобы `import`/`export` оставались «как есть» и не переписывались TypeScript’ом (минимальное вмешательство).  
    В Vite-проектах это полезно, и в связке с `"moduleResolution": "bundler"` даёт современное поведение. Для Next это тоже ок.
    
- `"allowImportingTsExtensions": true`  
    Разрешает писать `import foo from "./foo.ts"` или `.tsx` и т.д.  
    По дефолту TS ругается на такие импорты, но ты их явно разрешал.  
    Next это переварит, если сборщик сконфигурирован корректно, но в проде всё равно лучше стараться импортировать без расширения.
    
- `"skipLibCheck": true`  
    TypeScript не будет полностью проверять `.d.ts` из зависимостей.  
    Это:
    
    - сильно ускоряет сборку,
        
    - иногда скрывает баги в чужих типах — но это нормальный компромисс.  
        У тебя в обоих конфигах это включено — и в Next-проектах оно почти всегда включено.
        

---

### 🧱 Строгий режим и проверки

- `"strict": true`  
    Включает набор флагов строгой типизации (noImplicitAny, strictNullChecks и т.д.).  
    У тебя он уже есть в обоих tsconfig — это отлично, Next это любит.
    
- `"noUnusedLocals": true`  
    Ошибка, если есть неиспользуемые локальные переменные/константы.  
    Помогает держать код чистым. Ты это включил в обоих.
    
- `"noUnusedParameters": true`  
    Ошибка, если есть параметры функций, которые нигде не используются.  
    У тебя:
    
    - в app-конфиге было `false`,
        
    - в node-конфиге — `true`.  
        В Next-конфиге я поставил **`true`**, т.к. это более строгий и единый стиль. Если окажется слишком раздражающим — можно отключить обратно.
        
- `"noFallthroughCasesInSwitch": true`  
    Запрещает неявный «протекание» из одного case в следующий, если нет `break`/`return`.  
    Хорошая страховка от тупых багов. У тебя в обоих уже стоит.
    
- `"noUncheckedSideEffectImports": true`  
    Предупреждает/запрещает импорты, которые используются только ради сайд-эффектов и не имеют экспортов, с которыми TS может работать.  
    Это относительно новый флаг, повышает аккуратность импорта «голых» файлов с сайд-эффектами. Ты его использовал — я оставил.
    

---

### 🔧 Прочие флаги

- `"useDefineForClassFields": true`  
    Современное поведение полей классов (использует `define` семантику, как в стандарте JS), вместо старого, исторического.  
    Было у тебя в app-конфиге — и это нормальный флаг, безопасный для Next.
    
- `"erasableSyntaxOnly": true`  
    Говорит TS генерировать ошибки только на синтаксисе, который будет стёрт при компиляции (types, interfaces и т.д.), а не на чистом JS-коде.  
    В node-конфиге он стоял как `true`, в app — `false`. В Next я оставил `true` — это современная, чуть более строгая схема, но если IDE будет вести себя странно, можно вернуть `false`.
    

---

### 📂 include / exclude

- `"include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"]`
    
    - `next-env.d.ts` — файл с типами Next.js и React, который Next сам создаёт.
        
    - `**/*.ts`, `**/*.tsx` — весь твой TS/TSX-код в проекте.  
        В Vite у тебя было:
        
    - `["src"]` (app)
        
    - `["vite.config.ts"]` (node)  
        В Next простой глобальный `include` обычно удобнее.
        
- `"exclude": ["node_modules"]`  
    Чтобы TS не лез в зависимости. Часто ещё добавляют `.next`, но он по умолчанию и так редко попадает в include-маски; можно добавить, если хочешь:
    
    `"exclude": ["node_modules", ".next"]`
    

---

## 💡 Если хочешь чуть более «next-овый» вид

Можем ещё добавить alias, если у тебя есть `src/`:

`{   "compilerOptions": {     "baseUrl": ".",     "paths": {       "@/*": ["src/*"]     }     // ...остальное как было   } }`

И потом в `next.config.mjs` (если нужно, можно будет настроить alias под webpack/turbopack).